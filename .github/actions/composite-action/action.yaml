name: Composite Action 

inputs:
  greetings: 
    description: "Who to greet?"
    default: "Ayesha"
    required: true

outputs:
  random-number:
    value: ${{steps.gen-random-num.outputs.random}}

runs:
  using: composite
  steps:
    - name: Greetings
      run: echo "${{inputs.greetings}}"
      shell: bash
    - name: Random Number 
      id: gen-random-num
      run: |
        random=$RANDOM
        echo $random >> $GITHUB_OUTPUT
      shell: bash 




