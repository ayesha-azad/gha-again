name: TS JS Action Test 

on: push

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Check code
        uses: actions/checkout@v4
      - name: Testing JS Action
        uses: ./.github/actions/js-ts-action
        with:
          value: "Ayesha Azad"
